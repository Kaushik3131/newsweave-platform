import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ClerkProvider } from "@clerk/nextjs";
import { Toaster } from "@/components/ui/sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "NewsWeave - AI-Powered Newsletter Generator",
    template: "%s | NewsWeave",
  },
  description:
    "Create professional newsletters from RSS feeds using Google's Gemini AI. Curate content, customize settings, and generate engaging newsletters in minutes.",
  keywords: [
    "newsletter",
    "AI newsletter",
    "RSS feeds",
    "content curation",
    "Gemini AI",
    "email marketing",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <ClerkProvider>
      <html lang="en">
        <body
          className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        >
          {children}
          <Toaster />
        </body>
      </html>
    </ClerkProvider>
  );
}
